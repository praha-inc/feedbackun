import { style } from '@vanilla-extract/css';

export const wrapper = style({
  height: '100svh',
});

export const container = style({
  width: '100%',
  maxWidth: '1024px',
  margin: '0 auto',
  padding: '16px',
});
